Generation: 26
Elapsed time: 25415.47930407524sec
# of failed test cases in developer-fixed version: 4
# of failed test cases in current version: 4
List of applied operators:
ArraySubscripterOperator; if (':' === path[1] && '\\' === path[2 % path.length]) return true;
ArraySubscripterOperator; if (':' === path[1] && '\\' === path[2 % path.length % path.length]) return true;
ArraySubscripterOperator; if (':' === path[1 % path.length] && '\\' === path[2 % path.length % path.length]) return true;
ArraySubscripterOperator; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 % path.length : path.length - 1 : 0] && '\\' === path[2 % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 % path.length : path.length - 1 : 0] && '\\' === path[2 - path.length % path.length]) return true;
ArraySubscripterOperator; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 % path.length : path.length - 1 : 0] && '\\' === path[2 - path.length % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 % path.length : path.length % 1 : 0] && '\\' === path[2 - path.length % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length % 1 : 0] && '\\' === path[2 - path.length % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length % 1 : 0] && '\\' === path[2 * path.length % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length - 1 : 0] && '\\' === path[2 * path.length % path.length % path.length]) return true;
ArithmeticBinaryOperatorChanger; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length - 1 : 0] && '\\' === path[2 * path.length ** path.length % path.length]) return true;
ExprReplacerOperator; if (':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length - 1 : 0] && ':' === path[1 % path.length >= 0 ? 1 % path.length < path.length ? 1 ** path.length : path.length - 1 : 0]) return true;